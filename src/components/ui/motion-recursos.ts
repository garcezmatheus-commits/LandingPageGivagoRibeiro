// Carregado sob demanda pelo BlurText (LazyMotion): o motor de animação do
// Motion fica fora do JavaScript inicial da página.
import { domAnimation } from "motion/react";

export default domAnimation;
